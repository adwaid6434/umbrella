Required image files (place in this folder for images to display):

  • umbrella.png      — Hero section (recommended: ~750×750px or larger)
  • model-umbrella.png — Feature section (recommended: ~800×1200px or larger)
  • noise.png         — Subtle texture overlay (any size; used at 4% opacity)

Paths used in the app: /umbrella.png, /model-umbrella.png, /noise.png
